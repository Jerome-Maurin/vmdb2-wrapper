- All kernel in backports ?
- Dont use root= in /etc/default/flash-kernel, contents of /etc/fstab in initramfs should suffice instead
- Add correct support for uuid in initramfs fsck, see logs
- Add ansible role juste befor emptying /etc/resolv.conf
- On OrangePi R1, after ansible is added, add eth1 in dhcp to network/interfaces
- On NanoPi R1, after ansible is added, add eth1 in dhcp to network/interfaces
- Fix details of mount parameters to minimize writes to SD card (no journal, etc.)
- Add comments in yaml files
- Replace wget by proper apt install for armbian u-boots
